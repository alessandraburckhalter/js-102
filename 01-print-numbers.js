//Task: Write a function printNumbers which is given a start number and an end number. It will print the numbers from one to the other, one per line

function printNumbers(a, b) {
    for (let i = a; i <= b; i++) {
        console.log(i);
    }
}

printNumbers(1, 10);